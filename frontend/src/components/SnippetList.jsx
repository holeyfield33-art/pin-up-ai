// SnippetList.jsx\nexport default function SnippetList({ snippets, selectedId, onSelect }) {\n  return (\n    <div className=\"divide-y divide-slate-800\">\n      {snippets.map((snippet) => (\n        <button\n          key={snippet.id}\n          onClick={() => onSelect(snippet)}\n          className={`w-full text-left p-4 hover:bg-slate-800 transition ${\n            selectedId === snippet.id ? \"bg-slate-800\" : \"\"\n          }`}\n        >\n          <h3 className=\"font-semibold text-slate-100 truncate\">{snippet.title}</h3>\n          <p className=\"text-sm text-slate-400 line-clamp-2 mt-1\">\n            {snippet.body.substring(0, 100)}...\n          </p>\n          <div className=\"flex items-center gap-2 mt-2\">\n            {snippet.language && <span className=\"text-xs bg-slate-700 px-2 py-1 rounded\">{snippet.language}</span>}\n            <span className=\"text-xs text-slate-500\">{new Date(snippet.created_at).toLocaleDateString()}</span>\n          </div>\n        </button>\n      ))}\n    </div>\n  );\n}
